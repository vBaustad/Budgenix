import React from 'react';
import HeroSection from '../components/HeroSection';

const LandingPage: React.FC = () => {
    return (
        <div>
            <HeroSection />
            {/* Add more sections here later: FeatureSection, PricingSection, etc. */}
        </div>
    );
};

export default LandingPage;